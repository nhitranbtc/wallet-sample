[package]
name = "secure-storage"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
aes-gcm = { workspace = true }
zeroize = { workspace = true }
thiserror = { workspace = true }
async-trait = { workspace = true }
serde = { workspace = true }
rand = "0.8"

[features]
# Test-only helpers (e.g. `BiometricProof::granted_for_test`). Opt-in:
# production builds with `--no-default-features` or default features
# compile only the type and its `Granted` variant; downstream test
# targets enable this feature via their own dev-dependency declaration.
test-fixtures = []
